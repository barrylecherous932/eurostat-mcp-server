---
summary: "Metadata alignment: Dockerfile restored to oven/bun:1.3, package.json scripts/fields, manifest.json fields, server.json env vars, CLAUDE.md + AGENTS.md, vitest.config.ts simplified"
breaking: false
security: false
---

# 0.1.4 — 2026-05-23

## Changed

- **`Dockerfile`** — build stage restored to `oven/bun:1.3` (reverts the `node:24-slim` workaround); LABEL `title` and `description` aligned to the canonical package tagline.
- **`package.json`** — scripts updated to `bun run scripts/...` form; `start:stdio` / `start:http` use `bun ./dist/index.js`; `engines.bun` bumped to `>=1.3.2`; `files[]` trimmed to exclude `changelog/` and `AGENTS.md`; stale `start` script removed.
- **`manifest.json`** — `description` aligned to full canonical tagline; `repository`, `homepage`, and `license` fields added.
- **`server.json`** — `MCP_PUBLIC_URL` env var added to the HTTP package block.
- **`CLAUDE.md` / `AGENTS.md`** — synced to current framework template (header renamed "Agent Protocol", commands table updated to `bun run`, skills table current).
- **`vitest.config.ts`** — simplified to single `include` pattern; multi-project scaffold removed.
- **`devcheck.config.json`** — `tsx` added to depcheck ignores (used as shebang in `scripts/devcheck.ts` and `scripts/lint-mcp.ts`).
